import * as React from 'react';


const Formulario = () => {
  const [open, setOpen] = React.useState(true);

  const handleClick = () => {
    setOpen(!open);
  };

  return (
    <div />
  );
}

export default Formulario